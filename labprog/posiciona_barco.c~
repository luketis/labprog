#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "structs.h"
#include "leia_mapa.c"

#define MAX 10000;

void posiciona_barco(mapStruct map){
}


int main(){
    mapStruct map;
    char localDoArquivo[MAX]; 
    int B;

    scanf("%s %d", localDoArquivo, &B);

    map = leia_mapa(localDoArquivo);

    posiciona_barco(map);
    
    
    return 0;
}
